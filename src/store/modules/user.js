const state = {
    userInfo: {},
};

const mutations = {
    // 登录状态保存
    SET_LOGIN: (state, userInfo) => {
        debugger
    },
};

const actions = {
    logout({ commit, dispatch }) {
        debugger
    }
};

export default {
    namespaced: true,
    state,
    mutations,
    actions
};